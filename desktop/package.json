{
  "name": "@webrain/desktop",
  "version": "0.1.0",
  "description": "WeBrain desktop shell — Tauri 2.x wrapper around the existing dual-brain services.",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "check": "cd src-tauri && cargo check"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
